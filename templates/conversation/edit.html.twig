{% extends 'base1.html.twig' %}

{% block title %}Edit Conversation{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Edit Conversation</h1>

        <div class="d-flex justify-content-center">
            <div style="width: 50%; max-width: 600px;">
                <table class="table table-striped table-bordered">
                    <tbody>
                        <tr>
                            <td colspan="2">
                                {{ include('conversation/_form.html.twig', {'button_label': 'Update'}) }}
                            </td>
                        </tr>
                        <tr>
                            <td class="text-center">
                                <a href="{{ path('app_conversation_index') }}" class="btn btn-secondary">Back to list</a>
                            </td>
                            <td class="text-center">
                                {{ include('conversation/_delete_form.html.twig') }}
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}